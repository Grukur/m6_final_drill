<section>
    <table class="table">
        <thead>
            <tr class="table-dark text-center">
                <th scope="col">ID</th>
                <th scope="col">Nombre</th>
                <th scope="col">Genero</th>
                <th scope="col">año</th>
                <th scope="col">Autor</th>
            </tr>
        </thead>
        <tbody>
            <tr class="fw-bold text-center">
                {{#each data}}
                <th class="bgOwn text-light" scope="row">{{this.id}}</th>
                <td class="bgOwn text-light">{{this.nombre}}</td>
                <td class="bgOwn text-light">{{this.genero}}</td>
                <td class="bgOwn text-light">{{this.año}}</td>
                <td class="bgOwn text-light">{{this.autor}}</td>
                {{/each}}
            </tr>
        </tbody>
    </table>
</section>

<div class="text-center">
    <a class="btn btn-primary" href="/animes">Volver</a>
</div>

<script>


</script>