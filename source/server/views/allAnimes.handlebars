



{{!-- Enviamos data recibida a nuestra tabla --}}
<section class="">
    {{#if target}}
        {{>table
            data=target
        }}
    {{else}}
        <h2>Se produjo un error...</h2>
    {{/if}}
</section>
</div>